pipeline {
    agent {
        any {
            image 'jenkins/agent:latest'
            label 'docker-agent'
        }
    }
    stages {
        stage('Build') {
            steps {
               sh 'docker ps'
            }
        }
    }
}