pipeline {
    agent {
        docker { image 'docker:dind' }
    }
    stages {
        stage('Clone repository') {
            steps {
                checkout scm
            }
        }
        stage('Build image') {
            steps {
                sh 'docker ps'
            }
        }
    }
}